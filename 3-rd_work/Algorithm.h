#pragma once
#include <iostream>
#include <fstream>
#include "Matrix.h"
#include <cstring>

using namespace std;

void bubble_sort(Matrix matrix);

void insertion_sort(Matrix matrix);

void selection_sort(Matrix matrix);

void shell_sort(Matrix matrix);

void quick_sort(Matrix matrix);